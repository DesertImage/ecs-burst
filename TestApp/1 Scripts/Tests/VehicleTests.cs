using NUnit.Framework;
using Unity.Mathematics;

namespace DesertImage.Game
{
    public unsafe class VehicleTests
    {
        [Test]
        public void CombinedSlip()
        {
            // var combinedSlip = new float2(1f, -1f);
            // var normalizedCombined = math.normalize(combinedSlip);
            // var length = math.length(normalizedCombined);
            //
            // Assert.AreEqual(1f, length);
            // Assert.AreEqual(.5f, normalizedCombined.x);
            // Assert.AreEqual(.5f, normalizedCombined.y);
        }
    }
}